/**:
    ros__parameters:
        feature_extract_enable: false
        point_filter_num: 3
        max_iteration: 3
        # 下采样尺寸: 值越小精度越高但算力越重
        # 默认 0.5m (室外保守), 本项目室内 + Jetson AGX Orin 算力充足，调到 0.3/0.4
        # 若 CPU 占用过高 (>80%) 可回到 0.5/0.5
        filter_size_surf: 0.3
        filter_size_map: 0.4
        cube_side_length: 1000.0
        runtime_pos_log_enable: false
        map_file_path: "./test.pcd"

        common:
            lid_topic:  "/livox/lidar"
            imu_topic:  "/livox/imu"
            time_sync_en: false         # ONLY turn on when external time synchronization is really not possible
            time_offset_lidar_to_imu: 0.0 # Time offset between lidar and IMU calibrated by other algorithms, e.g. LI-Init (can be found in README).
                                        # This param will take effect no matter what time_sync_en is. So if the time offset is not known exactly, please set as 0.0

        preprocess:
            lidar_type: 1                # 1 for Livox serials LiDAR, 2 for Velodyne LiDAR, 3 for ouster LiDAR, 4 for any other pointcloud input
            scan_line:  4
            blind: 0.1
            timestamp_unit: 3
            scan_rate: 10
            # 尾部盲区 (跟随者过滤): 实验时操作者跟随在机器狗后方,
            # 其身体会被LiDAR扫描到, 在地图中产生"鬼影"并干扰导航避障
            # 后方对应LiDAR坐标系X轴负方向
            #
            # 参数选择建议 (见实验手册 §10.6):
            #   - 远距离跟随 (≥3m): half_angle=30°, range=5.0m (默认)
            #   - 近距离跟随 (1-2m): half_angle=40°, range=2.5m (当前配置)
            #   - 无人跟随: 关闭 tail_fov_blind_en
            tail_fov_blind_en: true       # 跟随者过滤开关
            tail_fov_half_angle: 40.0     # 盲区半角(度): 后方±40°=80°扇区 (近距离跟随)
            tail_fov_blind_range: 2.5     # 盲区最大距离(米): 只过滤近处，远处结构保留

        mapping:
            acc_cov: 0.1
            gyr_cov: 0.1
            b_acc_cov: 0.0001
            b_gyr_cov: 0.0001
            fov_degree:    360.0
            det_range:     100.0
            extrinsic_est_en:  true      # true: enable the online estimation of IMU-LiDAR extrinsic
            extrinsic_T: [ -0.011, -0.02329, 0.04412 ]
            extrinsic_R: [ 1., 0., 0.,
                            0., 1., 0.,
                            0., 0., 1.]

        publish:
            path_en: true                # true: publish Path
            effect_map_en: false         # true: publish Effects
            map_en: true                 # true: publish Map cloud
            scan_publish_en:  true       # false: close all the point cloud output
            dense_publish_en: false      # false: low down the points number in a global-frame point clouds scan.
            scan_bodyframe_pub_en: true  # true: output the point cloud scans in IMU-body-frame

        # 里程计位置约束配置 (用于抑制几何退化场景下的漂移)
        # 原理: 在ESEKF点面更新之后，追加一个基于外部融合里程计的XY位置约束更新
        # 退化判据: 有效特征点数 < feat_threshold 或 平均残差 > residual_threshold
        #
        # 阈值调优建议 (见实验手册 §10.1):
        #   1. 先开 runtime_pos_log_enable:true 跑一段开阔环境，观察 "effect num" 的正常量级
        #      (典型 300-800)，把 feat_threshold 设为正常值的 30-50%；
        #   2. 残差阈值应略高于正常场景下 res_mean 的 2-3 倍 (典型 0.05-0.1 → 取 0.15)。
        #   当前默认 (200 / 0.15) 针对室内走廊退化场景调高过 FAST-LIO 原始阈值 (50 / 0.3)，
        #   避免退化段几乎不触发导致消融实验"改进未生效"。
        odom_constraint:
            enable: false                             # 里程计约束总开关 (默认关闭，消融实验时通过 replay launch 覆盖)
            odom_topic: "/odometry/filtered"           # robot_localization 输出的融合里程计话题
            degradation_feat_threshold: 200            # 有效特征点数阈值 (低于此值判定为退化)
            degradation_residual_threshold: 0.15       # 平均残差阈值 (高于此值判定为退化)
            odom_noise_normal: 10.0                    # 正常状态: XY量测标准差(m), 代码中平方为R
            odom_noise_degraded: 0.1                   # 退化状态: XY量测标准差(m), 小=高权重
            odom_timeout: 0.5                          # Odom 超时阈值(秒): EKF 崩溃/停发 > 此时长则跳过约束 (防止 stale 数据拖偏)

        pcd_save:
            pcd_save_en: true
            interval: -1                 # how many LiDAR frames saved in each pcd file;
                                        # -1 : all frames will be saved in ONE pcd file, may lead to memory crash when having too much frames.
